version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = "1.8"
targetCompatibility = "1.8"

subprojects { project ->
    sourceCompatibility = "1.8"
    targetCompatibility = "1.8"
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile group: 'commons-io', name: 'commons-io', version: '2.4'
    compile group: 'com.mpatric', name: 'mp3agic', version: '0.8.1'
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.0'
    compile group: 'org', name: 'jaudiotagger', version: '2.0.3'
}

task FXPlayer(type: Jar) {
    manifest {
        attributes 'Implementation-Title': 'FXPlayer',
                'Implementation-Version': version,
                'Main-Class': 'com.joshuarichardson.fxplayer.Main'
    }
    baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}
